## Student Exam Performance Pridictor - ML Project

# For a Separate environment
    create env : conda create -p venv python=3.12 -y
    activate : conda activate .\venv
    deactivate : conda deactivate

# To install all requirements
    pip install -r requirements.txt

# To run the train pipeline
    python main.py
    or 
    python src/pipelines/train_pipeline.py

# To run flask app
    python app.py
    it will run the app on [http//localhost:5000](http://localhost:5000/predictdata)

# File Structure 
    ML_Project/
    ├── logs/
    ├── artifacts/
    ├── notebook/
    ├── src/
    ├── templates/
    ├── .gitignore
    ├── README.md
    ├── app.py
    ├── main.py
    ├── requirements.txt
    ├── setup.py
    
    Directory & File Explanations

# logs/
    all the logs are created here for checking the progress of the code and trobolshooting

# artifacts/
    Contains output files generated by the machine learning workflow, such as:
    Saved models
    Preprocessed data
    Purpose: Acts as a storage area for all artifacts created during training/evaluation so you don’t retrain or recompute every time.

# notebook/
    Holds Jupyter Notebooks:
    Exploratory Data Analysis (EDA)
    Data visualization
    Experimentation with models
    Purpose: Interactive experimentation and documentation of data/model behavior.

# src/
    Contains the core source code of the project.
    Common sub-components might be:
        Data loading & preprocessing functions
        Model training & evaluation scripts
        Utility modules
        Purpose: Houses reusable and production-ready code for ML pipelines.

# templates/
    Likely contains template files used by the app (if there’s a web interface) such as:
        HTML templates
        UI components
    Purpose: Used for rendering front-end pages if the project includes a web app/visualization interface.

# app.py
    A Python script — likely the entry point for running the application.
    Typical roles might be:
    Serving a model via an API (Flask, FastAPI, etc.)
    Running a training/testing pipeline
    Connecting UI templates with backend logic

# main.py
    Another Python entry script — often used to:
    Launch the main workflow (data → model → report)
    Trigger command-line execution with configurable options
    Purpose: Acts as the main controller for executing the project.

# requirements.txt
    Lists the Python packages/dependencies needed for the project.
    Purpose: Allows others to install all required libraries in one step (e.g., pip install -r requirements.txt).

# setup.py
    A Python setup script that can make the project installable as a package.
    Purpose:
        Defines metadata, dependencies, and packaging details.
        Enables pip install -r . for editable installs during development.

# How This Fits Together
    This repository appears to be a machine learning project with both modeling code and a runnable application, structured in a straightforward way:
    Data exploration & prototyping happens in notebook/
    Core model logic lives in src/
    Generated outputs are saved under artifacts/
    Frontend (if any) templates in templates/
    Executable entry points are app.py and main.py
    Documentation & environment setup handled via README.md, requirements.txt, and setup.py

# This structure helps separate:
    development (notebooks),
    core logic (src),
    application/interface (app.py + templates),
    outputs (artifacts),
    and environment configuration (requirements/setup).
